# Zuvote
This is an attempt to implement paystack in my app

i cant add the paystack reusable app in my settings.py
it throws this error

 File "C:\Users\IniEtim\AppData\Local\Programs\Python\Python39\lib\site-packages\django\apps\config.py", line 228, in create
    if not issubclass(app_config_class, AppConfig):
TypeError: issubclass() arg 1 must be a class
